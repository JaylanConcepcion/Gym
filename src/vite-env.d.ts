/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Injected at build time from the git commit count (see vite.config.ts). */
declare const __APP_VERSION__: string;
